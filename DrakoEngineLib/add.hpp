#pragma once
#include "exported.hpp"

namespace DrakoEngine {

    /// @brief Adds two integers together returning the result.
    /// @param a The first number.
    /// @param b The second number.
    /// @returns `a+b`
    DRAKOENGINELIB_EXPORTED int add(int a, int b);

}
